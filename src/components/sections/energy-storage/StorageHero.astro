---
import Container from "../../primitives/Container.astro";
---
<section
  class="page-hero relative isolate overflow-hidden bg-primary-dark text-white flex flex-col"
  aria-labelledby="storage-hero-title"
>
  <div class="absolute inset-0 -z-10" aria-hidden="true">
    <img
      src="/images/solar-storage-hero.webp"
      alt=""
      class="h-full w-full object-cover opacity-90"
      loading="eager"
      decoding="async"
      onerror="this.remove()"
    />
    <div class="absolute inset-0 bg-gradient-to-l from-primary-dark/95 via-primary-dark/75 to-primary-dark/40"></div>
    <div class="absolute inset-0 bg-gradient-to-t from-primary-dark/70 via-transparent to-transparent"></div>
    <div class="absolute -top-32 -right-32 h-[460px] w-[460px] rounded-full bg-accent/20 blur-[140px] animate-pulse-glow"></div>

    <div class="hidden lg:block absolute right-6 top-1/2 -translate-y-1/2 w-[380px] h-[360px] opacity-[0.10] animate-float">
      <svg width="100%" height="100%" viewBox="0 0 240 220" fill="none" preserveAspectRatio="xMidYMid meet">
        <rect x="70" y="40" width="100" height="140" rx="6" stroke="#F2994A" stroke-width=".8"/>
        <rect x="70" y="40" width="100" height="28" rx="6" stroke="#F2994A" stroke-width=".8" fill="rgba(242,153,74,.05)"/>
        <rect x="70" y="76" width="100" height="28" stroke="#F2994A" stroke-width=".5" fill="rgba(242,153,74,.04)"/>
        <rect x="70" y="112" width="100" height="28" stroke="#F2994A" stroke-width=".5" fill="rgba(242,153,74,.03)"/>
        <rect x="70" y="148" width="100" height="28" stroke="#F2994A" stroke-width=".4"/>
        <rect x="100" y="28" width="18" height="12" rx="3" stroke="#F2994A" stroke-width=".8"/>
        <rect x="122" y="28" width="18" height="12" rx="3" stroke="#F2994A" stroke-width=".8"/>
        <path d="M132 48 L126 60 L131 60 L125 72 L137 56 L131 56 Z" fill="#F2994A" opacity=".6"/>
      </svg>
    </div>
  </div>

  <Container class="relative flex-1 flex items-start pt-32 sm:items-center sm:pt-0">
    <div class="pb-16 sm:py-20 lg:py-24 max-w-3xl">
      <p class="eyebrow !text-accent hero-enter hero-enter-1">Energy Storage Systems</p>
      <h1 id="storage-hero-title" class="fluid-display mt-4 text-white hero-enter hero-enter-2">
        Store energy.<br/>
        <span class="text-accent">Maintain supply. Operate continuously.</span>
      </h1>
      <p class="fluid-lead mt-6 text-white/80 max-w-2xl hero-enter hero-enter-3">
        Battery energy storage (BESS) and advanced thermal storage solutions that stabilize renewable power, manage peak loads, enable 24/7 operation, and support true grid independence.
      </p>

      <div class="mt-8 flex flex-col sm:flex-row flex-wrap gap-3 hero-enter hero-enter-4">
        <a
          href="/projects?filter=storage"
          class="hero-home-pill"
        >
          View Storage Projects
        </a>
      </div>

    </div>
  </Container>
</section>

<style>
  .page-hero {
    min-height: calc(100vh - 6rem);
    min-height: calc(100svh - 6rem);
    min-height: calc(100dvh - 6rem);
  }
  @media (min-width: 1024px) {
    .page-hero {
      min-height: calc(100vh - 7rem);
      min-height: calc(100svh - 7rem);
      min-height: calc(100dvh - 7rem);
    }
  }
</style>
