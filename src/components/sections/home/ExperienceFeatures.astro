---
import Container from "../../primitives/Container.astro";

const features = [
  {
    title: "High-Efficiency Power Systems",
    description: "Advanced solar technology for maximum energy output and optimized performance.",
    icon: `<rect x="3" y="6" width="18" height="12" rx="1" fill="none" stroke="currentColor" stroke-width="1.5"/><path d="M3 10h18M3 14h18M9 6v12M15 6v12" stroke="currentColor" stroke-width="1.25"/>`,
    image: "/images/hero-solar.jpg",
    gradient: "from-primary-dark via-primary to-cta/40",
  },
  {
    title: "Commercial & Industrial Solar Solutions",
    description: "Scalable solar solutions for businesses, factories, and commercial facilities.",
    icon: `<path d="M3 21V9l7-4 7 4v12" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linejoin="round"/><path d="M14 21V13h5v8" fill="none" stroke="currentColor" stroke-width="1.5"/><path d="M6 14h4M6 17h4" stroke="currentColor" stroke-width="1.25" stroke-linecap="round"/>`,
    image: "/images/project-uae.jpg",
    gradient: "from-primary-dark via-primary to-accent/40",
  },
];
---
<section id="experience" class="section-pad bg-surface" aria-labelledby="experience-title">
  <Container>
    <div class="text-center max-w-2xl mx-auto reveal-stagger">
      <h2 id="experience-title" class="fluid-h2">Experience the Future of Solar Power.</h2>
      <p class="mt-3 text-muted fluid-lead">
        Reliable solar solutions engineered for performance, savings, and sustainability.
      </p>
    </div>

    <div class="mt-12 lg:mt-16 grid gap-5 lg:grid-cols-[1.2fr_1fr_1fr] lg:items-stretch reveal-stagger">
      <!-- Featured card: End-to-End Solar EPC -->
      <article class="group relative overflow-hidden rounded-2xl bg-primary-dark text-white shadow-card hover:shadow-card-lg transition-all duration-300 hover:-translate-y-1 min-h-[360px] lg:min-h-[440px]">
        <!-- Hover image layer — image only appears on hover, like the other cards -->
        <div class="absolute inset-0 opacity-0 group-hover:opacity-100 transition-opacity duration-500 ease-out pointer-events-none">
          <img
            src="/images/feature-epc.jpg"
            alt=""
            class="absolute inset-0 h-full w-full object-cover transition-transform duration-700 group-hover:scale-105"
            loading="lazy"
            decoding="async"
            onerror="this.remove()"
          />
          <div class="absolute inset-0 bg-gradient-to-br from-primary-dark/70 via-primary/50 to-success/30" aria-hidden="true"></div>
          <div class="absolute inset-0 bg-gradient-to-t from-primary-dark/75 via-primary-dark/20 to-transparent" aria-hidden="true"></div>
        </div>

        <div class="relative flex h-full flex-col justify-between p-6 lg:p-8">
          <div>
            <p class="text-[11px] font-semibold uppercase tracking-[0.22em] text-accent">End-to-End Solar EPC</p>
            <h3 class="mt-2 font-display text-2xl sm:text-3xl font-semibold leading-tight text-white">
              From Design to Commissioning
            </h3>
            <p class="mt-3 text-sm text-white/80 max-w-md">
              A single accountable partner for solar development, engineering, procurement, construction, and long-horizon asset performance.
            </p>
          </div>

          <div class="mt-6 flex items-end justify-between gap-4">
            <div>
              <p class="text-[11px] uppercase tracking-[0.2em] text-white/60">Savings:</p>
              <p class="mt-1 font-display text-3xl sm:text-4xl font-semibold text-white">
                Up to <span class="text-accent">60–70%</span>
              </p>
              <p class="text-xs text-white/70">Energy Cost Reduction</p>
            </div>

            <!-- Savings gauge -->
            <svg viewBox="0 0 120 120" class="h-20 w-20 lg:h-24 lg:w-24 shrink-0" aria-hidden="true">
              <defs>
                <linearGradient id="gauge" x1="0" x2="1" y1="0" y2="0">
                  <stop offset="0" stop-color="#F5B82E"/>
                  <stop offset="1" stop-color="#F27400"/>
                </linearGradient>
              </defs>
              <circle cx="60" cy="60" r="48" fill="none" stroke="rgba(255,255,255,0.12)" stroke-width="10"/>
              <circle
                cx="60"
                cy="60"
                r="48"
                fill="none"
                stroke="url(#gauge)"
                stroke-width="10"
                stroke-linecap="round"
                stroke-dasharray="301.6"
                stroke-dashoffset="90"
                transform="rotate(-90 60 60)"
              />
              <text x="60" y="66" text-anchor="middle" fill="#fff" font-family="Clash Display, sans-serif" font-size="22" font-weight="600">65%</text>
            </svg>
          </div>
        </div>
      </article>

      <!-- Two compact cards — default white, hover reveals photo background -->
      {features.map((f) => (
        <article class="group relative flex flex-col overflow-hidden isolate rounded-2xl bg-white border border-primary/10 shadow-card hover:shadow-card-lg transition-all duration-300 hover:-translate-y-1 hover:border-transparent p-6 lg:p-7 min-h-[360px] lg:min-h-[440px]">
          <!-- Hover image layer — pure image, no gradient overlay -->
          <div class="absolute inset-0 opacity-0 group-hover:opacity-100 transition-opacity duration-500 ease-out pointer-events-none -z-10">
            <img
              src={f.image}
              alt=""
              class="absolute inset-0 h-full w-full object-cover"
              loading="lazy"
              decoding="async"
              onerror="this.remove()"
            />
          </div>

          <span class="inline-flex h-14 w-14 items-center justify-center rounded-xl bg-accent-soft text-primary-dark ring-1 ring-accent/30 shadow-sm transition-all duration-300 group-hover:bg-white group-hover:ring-white">
            <svg viewBox="0 0 24 24" class="h-7 w-7" fill="none" set:html={f.icon}></svg>
          </span>
          <h3 class="mt-6 font-display text-lg lg:text-xl font-semibold text-primary-dark group-hover:text-white feature-text transition-colors duration-300">
            {f.title}
          </h3>
          <p class="mt-2 text-sm text-muted group-hover:text-white/95 feature-text leading-relaxed flex-1 transition-colors duration-300">
            {f.description}
          </p>
          <a
            href="#"
            class="mt-5 inline-flex items-center gap-1.5 text-sm font-semibold text-primary group-hover:text-accent feature-text-link transition-colors"
          >
            Learn more
            <svg viewBox="0 0 20 20" class="h-4 w-4 transition-transform group-hover:translate-x-1" fill="none" aria-hidden="true">
              <path d="M4 10h12m0 0l-4-4m4 4l-4 4" stroke="currentColor" stroke-width="1.75" stroke-linecap="round" stroke-linejoin="round"/>
            </svg>
          </a>
        </article>
      ))}
    </div>
  </Container>
</section>

<style>
  /* When the card is hovered, white type gets a subtle shadow so it reads
     against any image brightness without needing a darkening overlay. */
  .group:hover .feature-text {
    text-shadow: 0 1px 2px rgba(15, 23, 42, 0.6), 0 2px 8px rgba(15, 23, 42, 0.35);
  }
  .group:hover .feature-text-link {
    text-shadow: 0 1px 2px rgba(15, 23, 42, 0.55);
  }
  @media (prefers-reduced-motion: reduce) {
    .group:hover .feature-text,
    .group:hover .feature-text-link {
      text-shadow: none;
    }
  }
</style>
