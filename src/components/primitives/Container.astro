---
interface Props {
  as?: string;
  class?: string;
}
const { as: Tag = "div", class: className = "" } = Astro.props;
---
<Tag class={`mx-auto w-full max-w-container px-5 sm:px-8 lg:px-12 ${className}`}>
  <slot />
</Tag>
